<source>
  @type forward
  port 24224
  bind 0.0.0.0
  tag httpd.access
  format none
  <parse>
    @type json
  </parse>
</source>

<match httpd.access>
  @type stdout
  <format>
    @type json
  </format>
</match>
